[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm ci", "python3 -m venv /app/.venv", "/app/.venv/bin/pip install -r requirements.txt"]

[phases.build]

[start]
cmd = "npm start"
