[phases.setup]
nixPkgs = ["nodejs_18", "npm-9_x", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm ci", "pip install -r requirements.txt"]

[phases.build]

[start]
cmd = "npm start"
