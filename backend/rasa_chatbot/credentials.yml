# RASA Credentials Configuration
# Defines credentials for different chat channels

# REST Channel (for programmatic API access)
rest:
  # No credentials needed for REST channel

# Socket.IO Channel (optional - for WebSocket integration)
# socketio:
#   user_message_evt: user_uttered
#   bot_message_evt: bot_uttered
#   session_persistence: true
